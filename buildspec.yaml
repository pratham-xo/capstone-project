version: 0.2

phases:

  install:
    commands:
    - echo "Downloading SonarScanner..."

    - curl -L -o sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-8.1.0.6389.zip

    - unzip sonar-scanner.zip

    - echo "Current directory:"
    - pwd

    - echo "Files:"
    - ls -la

    - echo "Directories:"
    - find . -maxdepth 2 -type d

    - echo "Searching for sonar-scanner binary..."
    - find . -name sonar-scanner

    - chmod +x $(find . -name sonar-scanner)

    - export PATH=$PATH:$(dirname $(find . -name sonar-scanner))

    - echo $PATH

    - which sonar-scanner

    - sonar-scanner --version

    - echo "Installing Trivy..."

    - curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh

    - mv ./bin/trivy /usr/local/bin/

    - trivy --version
    - echo "Downloading Trivy vulnerability database..."
    - trivy image --download-db-only

  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - IMAGE_TAG=build-$CODEBUILD_BUILD_NUMBER
      - ECR_REGISTRY=$(echo $REPOSITORY_URI | cut -d'/' -f1)
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $ECR_REGISTRY

      - echo Retrieving SonarQube token from SSM...
      - export SONAR_TOKEN=$(aws ssm get-parameter --name "/sonarqube/token" --with-decryption --query Parameter.Value --output text)

  build:
    commands:
    - echo Running SonarQube Scan...

    - |
        sonar-scanner \
          -Dsonar.projectKey=capstone-project \
          -Dsonar.projectName="Capstone Project" \
          -Dsonar.sources=. \
          -Dsonar.host.url=$SONAR_HOST_URL \
          -Dsonar.token=$SONAR_TOKEN

    - echo "Build started on $(date)"
    - docker build -t $IMAGE_REPO_NAME:$IMAGE_TAG .
    - echo "Running Trivy scan..."

    - |
         trivy image \
          --exit-code 1 \
          --severity HIGH,CRITICAL \
          $IMAGE_REPO_NAME:$IMAGE_TAG

    - docker tag $IMAGE_REPO_NAME:$IMAGE_TAG $REPOSITORY_URI:$IMAGE_TAG
  post_build:
    commands:
      - echo Pushing image to ECR...
      - docker push $REPOSITORY_URI:$IMAGE_TAG

      - echo Creating imageDetail.json...
      - printf '{"ImageURI":"%s"}' "$REPOSITORY_URI:$IMAGE_TAG" > imageDetail.json

      - echo "Build completed on $(date)"
      - echo Image pushed as $IMAGE_TAG

  artifacts:
    files:  
      - appspec.yaml
      - taskdef.json
      - imageDetail.json

